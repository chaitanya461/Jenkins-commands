name: Deploying war to Tomcat Servers

hosts: all

tasks:

name: Copying war file to Tomcat Servers

copy:

/var/lib/jenkins/workspace/project/target/myapp.war

src: dest: /root/tomcat/webapps


---- mv deploy.yml to /etc/ansible
