---
- name: Deploying war to Tomcat Servers
  hosts: all
  tasks:
    - name: Copy myapp.war to Tomcat webapps directory
      copy:
        src: /var/lib/jenkins/workspace/job1/target/myapp.war
        dest: /root/tomcat/webapps/

---- mv deploy.yml to /etc/ansible
